> Calculator API

```bash
# env
copy .env.example to .env

# install dependencies
composer install

# db
config your database

# run migrations
php artisan migrate

# serve app at localhost:8000
php artisan serve
```
